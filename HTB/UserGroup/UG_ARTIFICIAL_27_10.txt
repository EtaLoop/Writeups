LINUX
Nmap -> port 80, http

Sign/Login -> dashboard
- Example code: lib tensorflow -> exploit "remote code execution" qui convertit le rev shell en fichier .h5

- Nc de notre coté + On upload le fichier

REVSHELL:
- Accès à app.py + user.db (ou il y a des creds encrypter en md5)
- john => obtient les creds de gael
- su gael = user.txt

- id: gael membre du groupe sysadm
Chercher des fichiers qui peuvent être accèder par ce groupe :
- find / -group sysadm 2> /dev/null -> backup.tar

- dans config.json on trouve password BCrypt qui n'est pas du bcrypt mais du base64 + db tourne sur 127.0.0.1 9898
- on décode le base64 et on obtient du bcrypt cette fois + john

-> creds de backrest_root

- avec ss -tulpn on voit les process qui tournent (sur le port 9898)

- Avec ssh -L on peut forward un port interne à un port externe
ssh -L 9898:127.0.0.1:9898
On a maintenant accès à 127.0.0.1:9898 sur firefox (creds backrest_root)

- On ajoute un repository -> local file = /backrest_root
- On ajoute un plan et on le lie au repo
-> partie run command -> help -> peut créer/lire backup
-> on créer une backup pour /root (tout /root est mis dans une backup)
-> on lit dans la backup <id>, le fichier /root/root.txt